import { expect } from "chai";
import rightSideView from "../lib/idbdyoung-rightSideView.js";

describe("permutation", function() {
  it("should return all possible combinations", function() {
    expect(rightSideView().to.eql());
  });
});
